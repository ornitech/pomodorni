#!/bin/sh
# Run tests before allowing commit

echo "Running tests..."
swift package resolve --quiet 2>/dev/null
if ! swift test --quiet 2>&1; then
  echo ""
  echo "ERROR: Tests failed. Fix failing tests before committing."
  echo ""
  exit 1
fi
